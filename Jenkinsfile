pipeline {
	agent any 
	stages {
		stage('build') {
			steps {
			echo"build"
			}
		}
		stage("tes") 
			steps {
			echo "test"
			}
		}

		stage("integration test") {
			steps {
			echo "integration test"
			}
		}
	}
}